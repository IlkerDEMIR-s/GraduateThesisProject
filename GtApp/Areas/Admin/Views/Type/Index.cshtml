@model List<ThesisType>

<div class="text-center">
    <div class="display-6 my-3 font-weight-bold" style="font-family: 'Arial', sans-serif;">Types</div>
    <table class="table table-striped table-hover my-3">
        <hr class="my-4" style="border: 2px solid black; border-radius: 5px;">
        <div class="text-center">
            <div class="d-flex justify-content-end my-3">
                <a class="btn btn-outline-success" asp-action="Create">
                    <i class="fa fa-plus"></i>
                    Create
                </a>
            </div>
        </div>
        <thead>
            <tr>
                <th class="font-weight-bold"><i class="fa-solid fa-hashtag"></i>Type Id</th>
                <th class="font-weight-bold">Type Name</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var type in Model)
            {
                <tr>
                    <td>@type.TYPE_ID</td>
                    <td>@type.TYPE_NAME</td>

                    <td>
                        <div class="btn-group">
                            <a class="btn btn-danger" asp-action="Delete"
                               asp-route-id="@type.TYPE_ID">
                                <i class="fa fa-trash"></i> Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
</div>
